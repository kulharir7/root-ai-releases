---
summary: "CLI reference for `Root uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `Root uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
Root uninstall
Root uninstall --all --yes
Root uninstall --dry-run
```
